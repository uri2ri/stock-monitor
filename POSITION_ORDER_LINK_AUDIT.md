# 기존 미확인 매도 주문 연결 점검

## 조회 결과 — 2026-09-15 05:37:28 KST

- 대상: 설정된 자동주문 기록 DB (`NOTION_ORDERS_DB_ID`).
- 조건: `계좌구분=모의`, `매매구분=매도`, `상태=주문중 또는 실패`.
- 방식: Notion DB query API를 읽기 전용으로 호출. 페이지네이션을 지원하며 이번 조회는 전체 결과가 빈 배열이었다.
- 미확인 매도 **0건**, 관계 없는 주문 **0건**, 복수 관계 주문 **0건**.
- 후보 연결표: 대상 없음. 관련 보유/청산 행과 매매일지의 추가 조회도 불필요하여 실행하지 않았다.
- 데이터 연결·주문 상태 변경·스키마 변경 **0건**. 증권사 조회/주문, 카카오 알림, 워크플로 수동 실행도 하지 않았다.

## 판단 근거와 한계

이번 조회 시점에는 기존 미확인 매도 주문의 관계 누락으로 발생하는 이관 선행 조건이 없다. 따라서 실제 데이터 이관 승인을 요청할 대상도 없다. 단, 이는 Notion의 해당 조건에 해당하는 기록이 없다는 뜻이며, 증권사 미체결 주문이 없거나 과거 성공 주문이 정확하다는 증거는 아니다. 성공·거부·경고 및 다른 계좌의 주문은 조회 범위 밖이다.

아직 새 코드를 main에 병합·배포하지 않았다. 운영 중 새 미확인 주문이 생성될 수 있으므로 병합/배포 직전에 같은 조건으로 재조회한다. 새 대상이 발견되면 주문번호·일시·계좌·종목·수량과 보유/청산 포지션·일지 증거를 대조한 후보표를 먼저 만들고, 실제 연결은 별도 승인받는다. 날짜나 동일 종목만으로 자동 연결하지 않는다.

## 재확인 절차

1. 같은 DB에 위 계좌/매매구분/상태 조건을 AND로 적용하고 모든 페이지를 읽는다.
2. 결과 0건이면 이관 대상 없음으로 기록한다. 오류나 조회 실패를 0건으로 처리하지 않는다.
3. 결과가 있으면 `보유종목` 관계 개수를 분류한다. 누락/복수 관계 및 연결 대상의 유효성을 검토한다.
4. 근거 부족 항목은 수동 확인으로 남기며 데이터 변경 없이 보고한다.

이번 변경은 문서만 수정했다. 코드 검증은 직전 커밋 `a24f2b6`의 네트워크 차단 테스트 **127 passed**를 참조하며, 실제 모의투자 연동 완료를 의미하지 않는다. 신규매수 상한 `MAX_ORDERS_PER_DAY=0`은 변경하지 않았다.
