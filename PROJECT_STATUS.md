# PROJECT STATUS

## P2 후속 체결 확인 및 P1 통합 검증 — 2026-09-20 (운영 미반영)

- 새 돌파가 없는 회차에도 별도 후처리 worker가 접수된 추적 건의 체결을 확인한다. 계좌 연결키/모의 구분·종목·매수·주문일·주문번호·요청수량을 대조한다. 기존 매매 경로의 주문 판단은 그대로다.
- 직후 미확인 → 다음 실행 전량 확인이면 추적만 종료한다. 부분체결은 확인수량/미확인잔여수량을 구분하고 추적을 유지한다. 조회실패·주문미발견·수량0관측을 구분한다. 취소·거부·만료를 시간/잔고로 추정하지 않는다.
- 후속 확인 위치: `auto-trade.yml`의 Notion 전달 단계 → `tools/breakout_worker.py deliver` → `breakout_reconcile.run()`. 주문·취소·정정·메시지 함수는 호출하지 않는다. 최대 5건/회차이며 추적 DB만 갱신하도록 준비했다.
- 관련 회귀 테스트 **110 passed (26.50초)**, 추가 보완 후 최종 외부 연결 차단 전체 테스트 **315 passed (26.02초)**. 새 프로세스 저널 복원, 생성 응답 유실, 부분 성공, 대상 없는 이벤트 보존, 기존 매매 격리, 날짜/계좌/주문 대조, 부분체결·조회실패·재실행을 포함한다.
- Python 구문 검사, `git diff --check`, Ruby YAML 파싱 및 checkpoint → deliver 단계 순서 확인 통과. 실제 workflow 실행은 하지 않았다.
- 작업 브랜치 `codex/settlement-breakout-review`. P1 커밋 `73065a7`, 대상 미확인 이벤트 보존 보완 `9f0270a`; P2는 별도 목적 커밋으로 기록한다. 기준 `7d05ed0`의 기존 작업은 보존했다.
- 남은 운영 조건: 별도 추적 DB에 `관측ID`와 계좌/수량/체결조회 속성 및 `부분체결` 선택지 필요. 코드·설정 문서만 갱신했으며 운영 스키마·시크릿은 변경하지 않았다.
- 실제 KIS TR/조회 지원 여부는 기존 미확인 상태이며 시험 호출하지 않았다. 생성 여부 불명확한 미발견 계획은 수동 확인이 필요하다. 첫 Git checkpoint 전 러너 소실은 무손실 보장 범위 밖이다.
- main 병합·배포·워크플로 수동 실행·실제 API 시험·주문·메시지·운영 Notion 변경 없음. 신규매수 상한 0과 매도 전략 유지.

## P1 영속 이벤트 복구 — 2026-09-20 (운영 미반영)

- 메모리 큐를 비우던 경로를 로컬 fsync 저널과 별도 prepare/checkpoint/deliver 단계로 교체했다. Git commit/push가 실행 간 복원 원본이며 14일 artifact는 보조 수동 복구 자료다.
- 관측ID 대조로 생성 응답 유실을 확인한다. 확인 불가 생성은 자동 재전송하지 않고 `ambiguous`로 보류한다. 성공한 계획만 제거하며 최초값과 실패 이벤트를 보존한다.
- 로컬 저장 실패, 첫 push 전 강제 종료, checkpoint 실패 시 유실/수동 복구 한계를 [BREAKOUT_DURABILITY.md](BREAKOUT_DURABILITY.md)에 명시했다. 무손실을 보장한다고 보고하지 않는다.
- 별도 추적 DB 속성/시크릿 준비 필요. 운영 DB 변경·실제 API 시험·워크플로 실행·병합·배포 없음. 후속 P2 검증 결과는 상단 최신 기록에 추가한다.

## 돌파 후 미진입 추적 마무리 — 2026-09-19 (운영 미반영)

이전 세션이 남긴 WIP(`acead1a` + worktree 미커밋분)를 이어받아 마감했다.
기존 미커밋 변경은 지우거나 덮어쓰지 않고 그 위에 얹었다. `reset`·force-push·
브랜치 삭제는 하지 않았다.

### 완료

- **미진입 분류를 호출부가 명시하도록 바꿨다.** 이전 판은 사유 문자열에
  `'현금 부족'`·`'상한'`이 들어 있는지로 `자금부족`/`상한제한`을 정했다.
  증권사 거부 메시지(`주문 거부: …`)에 "상한가"가 섞여 오면 자금·상한과
  무관한 건이 상한제한으로 둔갑한다. 이제 `record_no_entry(..., category=)`를
  게이트가 직접 지정하고, 모르는 분류는 `미주문`으로 떨어진다. 사유 원문은
  그대로 남는다. (`가격 상한 초과`는 유닛 상한과 다른 사건이라 `미주문`이다.)
- **야간 스캔이 통째로 없는 경우와 특정 종목만 없는 경우를 구분한다.** 이전
  판은 스캔 파일이 없어도 종목마다 "유동성 컷·거래정지·상장폐지"라는 원인을
  적었다 — 인프라 장애를 종목 사유로 기록하는 셈이었다.
- 확정 종가 기준일 검증에서 `최초감지`가 없거나 타입이 깨진 행을 예외 대신
  `갱신실패`로 처리한다. 확정 종가 하한 시각은 `CLOSE_CONFIRMED_HOUR`(16시)로
  이름을 붙였다.
- `close_track()`이 시간대 없는 datetime을 조용히 받지 않는다(`local_time`).
- 웹앱 추적 탭이 `sort_tracks()`를 거치고, 조회 실패를 "대상 없음"으로
  보여주지 않는다. `mailer`·`daily_report`의 추적 import를 모듈 상단으로
  올려 import 실패가 리포트 발송 자체를 막지 않게 했다.
- 노션 `주문번호`·`주문일` 칸을 `BT_ORDER_NO`/`BT_ORDER_DAY` 상수로 정리했다.
- 아침 리포트 요약 머리줄에 가격 갱신 실패 건수를 붙였다(활성 건 없으면 생략).
- **[BREAKOUT_TRACKING.md](BREAKOUT_TRACKING.md) 신규.** 코드가 두 군데서
  참조하는데 파일이 없었다. Notion DB 스키마(칸 이름·타입·선택지), 환경변수,
  동작 규칙, 격리 방식, 미결정 사항을 적었다. `.env.example`에도 두 키를 넣었다.
- **테스트 285 passed** (외부 연결 차단, 이전 266 → 신규 19건).

### 확인한 것 (이전 세션 작업 중 검증 완료)

- `새돌파` 판정이 `high20`(당일 제외 20일 고가)을 쓴다. `high20_next`(당일 포함,
  다음 거래일 기준선)와 비교하면 당일 고가가 자기 자신을 못 넘어 새 돌파가
  항상 불가능해지는데, 그 오류는 없다. 두 값 모두 `scan_latest.csv`에 있다.
- 주문 경로에서 추적 저장소를 동기로 부르지 않는다(메모리 큐 → 회차 끝
  `flush_events`). 운영 저장소를 붙인 채 `run_auto_trade`를 돌려 저장소 호출
  0회를 확인했다.
- 추적이 꺼져 있든 모든 호출이 터지든 `select_buy_candidates`의 선정 결과와
  주문 호출은 동일하다.
- 체결 결과는 `주문번호 + 주문일`이 그 추적 건의 것과 일치할 때만 반영된다 —
  종목만 같은 옛 주문이 새 추적 건을 닫지 않는다.

### 미완료 · 운영 미반영

- **운영 Notion 추적 DB를 만들지 않았다.** 시크릿(`NOTION_BREAKOUT_TRACK_DB_ID`)도
  넣지 않았다. 없으면 기능 전체가 비활성이고 워크플로는 실패하지 않는다.
- 워크플로(auto-trade / daily / nightly-scan)에 환경변수 배선만 해 뒀고
  **수동 실행하지 않았다.** 실제 API 호출·주문·메시지·운영 Notion 변경 없음.
- **SP삼화(000390)의 과거 돌파 이력은 넣지 않았다.** 최초 감지 시각과 그때의
  기준선·ATR을 남은 근거로 복원할 수 없어 없는 값을 만들어 넣지 않았다.
  기능을 켠 뒤 다음 관측부터 쌓인다.
- **자동 만료 기간은 미결정이다.** 근거 없이 숫자를 정하지 않았다. 종료는
  체결 확인 또는 사용자 종료로만 된다.
- 동시 실행 중복 생성은 **원자적으로 막지 못한다.** Notion에 유일키가 없어
  "조회 후 생성"만으로는 부족하다. 쓰기 주체를 `BREAKOUT_TRACK_WRITER=auto-trade`
  하나로 묶고 기존 concurrency 그룹의 직렬화에 기댄 완화책이다. 중복된 '추적중'
  행이 생기면 조회가 예외를 올려 자동 갱신을 멈추므로 사람이 정리해야 한다.
- 매도 전략·매매 설정(신규매수 상한 0 포함)·진입/추가매수/청산 조건은
  건드리지 않았다. 카카오 알림은 추가하지 않았다.

### 화면

`tools/breakout_preview.py`(가상 데이터, `.env`·운영 기록을 읽지 않는다)로 확인:
[목록](docs/breakout-preview.png) · [상세](docs/breakout-detail.png).

## 청산 증빙 안전 보완 — 2026-09-18 (운영 미반영)

- 작업 기반: `origin/claude/breakout-no-entry-tracking`의 `acead1a`. `01f3599`, `f972a03`, `acead1a`는 점검한 원격 main `053012e`의 조상이 아니다. 기존 로컬 main과 미추적 사용자 파일을 보존하고 별도 worktree에서 작업한다.
- 잔고 0만으로 주문 전량체결·청산일·체결가를 추정하던 경로를 제거했다. 잔고 없음과 체결/회계 미확정을 구분하며 일지·청산·성공 갱신 없이 미확정 주문을 유지한다.
- 일지 작성 함수도 참고가 폴백을 금지한다. 실제 체결 증빙이 확보되면 기존 일지 → 보유 청산 → 주문 성공 순서를 사용한다.
- 외부 연결 차단 전체 테스트: 200 passed (17.20초). 추적 WIP의 검증 완료를 뜻하지 않는다.
- 운영 Notion·매매 설정 변경, 실제 API 시험 호출, 메시지, workflow_dispatch, 병합·배포 없음.

## GS 진단 사실 및 기존 승인 데이터

- GS(078930), 2026-09-17 매도 주문 `0000006229`, 13주: 사용자 체결 확인이 있었으나 주문별 실제 체결가격 증빙은 확보되지 않았다.
- 기존 보고의 현금 증가 관측은 정황이며 특정 주문의 수량·가격·체결일을 확정하는 증빙으로 쓰지 않는다.
- [09-17 조회 run 35329043238](https://github.com/uri2ri/stock-monitor/actions/runs/35329043238): HTTP 200, tr_cont E, rt_cd 0, msg_cd 70070000, output1 빈 배열. 확정 가능한 것은 해당 계좌·날짜·조건에서 조회 0건이라는 사실뿐이다.
- [09-18 조회 run 35329257578](https://github.com/uri2ri/stock-monitor/actions/runs/35329257578): 당일 주문이 없었다고 보고돼 당일 조회 지원 여부의 검증 근거가 아니다.
- 이전 문서의 “모의투자는 과거 체결 조회 불가능”, “잔고 확정이 유일한 복구 수단” 주장은 철회한다. 원인은 미확인이다. 상세 공식 샘플 대조는 [KIS_FILL_QUERY_REVIEW.md](KIS_FILL_QUERY_REVIEW.md).
- 이전 작업자가 사용자 승인으로 GS를 수동 정리했다고 기록했다: 진입가 120,600원, 13주, 청산일 09-17, 청산가 **113,000원은 10일 청산선 대체값(실제 체결가 아님)**, 진입일 08-20, 신호일 09-16. 보유를 청산, 주문을 성공으로 변경했다고 기록돼 있다.
- 해당 승인된 대체 장부는 이번에 조회·수정·삭제하지 않았다. 향후 실제 증빙과 대조하더라도 변경은 별도 승인 대상이다.
- 진단 도구 `01f3599`의 읽기 재시도·30초 타임아웃은 보존했다.

## 상태 점검 — 2026-09-16 (읽기 전용 확인, 코드 변경 없음)

- **작업 브랜치**: `claude/holdings-search-error-kfynqa` head `ca84cdb`,
  origin과 동기화 완료(미푸시 커밋·미커밋 변경 없음). `origin/main` 대비
  앞선 커밋은 재시도 관련 2건(`f3c61b5`, `ca84cdb`)뿐이고 **이 브랜치로
  열린 PR은 없다** — 커밋·푸시만 돼 있고 병합 대기열에는 올라가 있지 않다.
- **테스트 재실행**: `python -m pytest tests/ -q` → **160 passed**
  (아래 재시도 섹션에 적힌 수치와 일치). 확인만 했고 코드는 건드리지 않았다.
- **열린 PR**: #1 `fix/backtest-execution-accounting`(Draft, 2026-09-09
  이후 변동 없음) 하나뿐이며 이번 재시도 작업과는 무관하다.
- **`fix/auto-sell-recovery-review` 브랜치는 superseded**: 원격에 4커밋
  (`7b92e00`까지)이 남아 있고 여전히 main 미병합이다. 그 브랜치가 지적·
  수정했던 4건(워크플로 `NOTION_LEDGER_DB_ID` 누락, 날짜 경계로 끊기는
  복구, 미확정 주문 추적 순서, 수량·가격 유효성)은 **PR #4 계열의 포지션
  연결 구현으로 main에 이미 다른 방식으로 반영돼 있다** — `origin/main`에
  `_reconcile_sell`·`_recover_closed_sell`·`_valid_sell_qty`(kis_client.py),
  `fetch_unconfirmed_sell_orders`·`has_ledger_for_holding`·
  `ledger_matches_sell`(notion_repo.py), 워크플로의 `NOTION_LEDGER_DB_ID`가
  모두 존재함을 확인했다. 따라서 지금 그 브랜치를 병합하면 날짜 추정
  기반의 옛 구현으로 되돌리는 셈이라 **병합 대상이 아니다**. 브랜치 정리
  (삭제) 여부는 사람이 결정한다.
- 이번 항목은 문서만 추가했다. 병합·배포·워크플로 실행·주문·알림·노션
  데이터 변경은 하지 않았고, 신규매수 상한 0은 그대로다.

## 현재가·잔고 조회 재시도 추가 — 2026-09-15

- **문제**: KIS 조회가 일시적으로 실패하면 그 회차의 판정이 통째로 생략된다.
  워크플로는 성공으로 끝나 겉으로 드러나지 않는다. 2026-09-15 실행 로그
  (모두 한국시간):
  - 13:23 [run 34928611056](https://github.com/uri2ri/stock-monitor/actions/runs/34928611056)
    — `잔고조회 실패 … Read timed out. (read timeout=10)`으로 자동매도
    건너뜀(04:23:50 UTC), 15초 뒤 추가매수도 같은 사유로 건너뜀(04:24:05 UTC).
  - 15:03 [run 34935237791](https://github.com/uri2ri/stock-monitor/actions/runs/34935237791)
    — 잔고조회 시간초과로 자동매도 건너뜀(06:03:51 UTC). **13초 뒤 같은
    잔고조회가 정상 응답**(06:04:04 UTC, "계좌: 평가액 …")했다.
    이건 "후속 조회가 성공했다"는 사실일 뿐, **실패한 그 호출이 재시도로
    복구됐다는 증거는 아니다** — 재시도를 실제로 걸어본 적이 없으므로
    같은 시점에 다시 물었을 때 성공했을지는 확인되지 않았다. 재시도가
    도움이 될 **가능성**을 시사하는 정황으로만 읽는다.
  - 09:23·09:33·09:43 종목별 현재가 조회 HTTP 500 (사용자 제공 관측).
- **원인(추정)**: 로그에 남은 건 읽기 시간초과와 5xx뿐이고, 둘 다 이쪽
  코드의 요청이 잘못돼서 나는 오류는 아니다. **KIS 서버 측 또는 경로상
  네트워크의 일시적인 문제일 가능성이 높다** — 다만 KIS 측 장애 공지나
  상태 기록으로 확정한 것은 아니므로 "서버 장애 확정"으로 적지 않는다.
  분명한 건 이 코드가 첫 실패를 그대로 최종 실패로 다뤄 재시도 여지를
  쓰지 않았다는 점이다 — 토큰 발급에만 재시도가 있었고(`TOKEN_RETRIES`),
  현재가·잔고 조회에는 없었다.
- **수정**(`kis_client.py`, 읽기 경로 한정):
  - `_read_with_retry()` — 최초 호출 포함 **최대 3회**, 재시도 직전 대기
    1초 → 2초.
  - 적용 지점은 `_fetch_price_quote()`(현재가·시장경보)와
    `get_account_balance()`(잔고) **두 곳의 네트워크 호출뿐**이다.
    `get_current_price()`·`get_price_quote()`는 모두
    `_fetch_price_quote()` 하나를 지나므로 재시도가 중첩돼 호출 수가
    곱해지지 않는다. 전역 HTTP 재시도(Session/Adapter)는 쓰지 않았다.
  - **재시도 대상**: 연결 오류·읽기 시간초과, 일시적 5xx(500/502/503/504).
  - **재시도 안 함**: 4xx 전체. **429도 재시도하지 않는다** — KIS는 유량
    제한을 건 호출을 곧바로 받아주지 않고 재시도가 제한을 키운다(토큰
    발급이 4xx를 재시도하지 않는 기존 정책과 동일한 판단). 501·505,
    그리고 HTTP 200 안의 KIS 오류·불완전 응답(`rt_cd != '0'`,
    `stck_prpr` 없음, 연속조회 필요)도 제외했다 — 의미가 확인되지 않은
    코드를 일시 장애로 넘겨짚지 않고 기존 검증에 맡겨 보류시킨다.
  - **최종 실패 시** 마지막 예외를 그대로 올려 기존 예외 처리(판정 보류·
    경고)로 넘긴다. 빈 잔고·가격 0·오래된 가격으로 대체하거나 검증을
    우회하지 않는다. 매도 수량은 **성공한 최신 잔고 응답**의
    `ord_psbl_qty`를 쓰고, 기존 체결 확인·포지션 관계·중복 주문 방지
    로직은 그대로다.
  - **재시도 시간 예산**: `READ_RETRY_TOTAL_BUDGET_SECONDS = 60`. 자동매도 +
    추가매수 + 신규매수가 한 프로세스를 공유하므로 재시도에 쓴 시간을
    전역으로 누적하고, 예산을 넘으면 이후 조회는 재시도 없이 1회로
    떨어진다. 남은 예산은 재시도 묶음을 시작할 때뿐 아니라 **매 재시도
    대기 직전과 대기 뒤 호출 직전에도** 다시 확인한다(2026-09-16 보완).
    도중에 소진되면 거기서 멈추고 마지막 조회 오류를 그대로 올려 기존
    보류 경로로 넘긴다.
    이 예산은 재시도를 억제하는 **근사 상한이지 전체 실행시간의 엄밀한
    상한이 아니다**. `requests`의 `timeout=10`은 연결·읽기 각 단계의
    무응답 한도일 뿐 한 요청의 총 소요를 10초로 묶어주지 않고(서버가
    응답을 천천히 흘려보내면 그만큼 길어진다), 진행 중인 호출을 중간에
    끊지도 않는다. 따라서 이전 판에 적었던 "추가 소요 상한 60 + 23 ≈
    83초"는 **성립하지 않는 보장이라 철회한다** — 초과분은 "마지막으로
    시작한 호출 하나" 만큼 남을 수 있다. 참고로 위 두 실행의
    `intraday_watch.py` 단계 실측은 각각 42초·78초이고, 최종 안전장치는
    워크플로 제한 8분(`intraday.yml`·`auto-trade.yml`)이다.
  - 주문 전송·정정·취소, 노션 쓰기, 카카오·이메일 발송에는 재시도를
    **추가하지 않았다**.
  - 로그는 시도 횟수·예외 **종류만**(`type(e).__name__`) 남긴다 —
    자격증명·계좌번호·응답 본문은 출력하지 않는다.
- **검증 A — mock 단위 테스트(완료, 이번 변경으로 확인한 전부)**:
  `python -m pytest tests/ -q` → **160 passed** (신규
  `tests/test_read_query_retry.py` 33건 포함, 기존 127건 전부 유지).
  실 HTTP는 `requests`의 전 메서드 + `Session.request`를 막고, 자격증명
  환경변수는 테스트 시작 때 제거한 뒤 더미만 넣는다. 재시도 대기와
  `time.monotonic()`은 가짜 시계로 대체한다. 확인한 것:
  최초 성공 시 1회 호출, 일시 실패 후 복구, 최대 횟수에서 정지, 재시도
  대상 아닌 오류·무효 응답의 즉시 실패, 예산이 **이미** 소진된 경우 1회
  호출로 강등, 예산이 **재시도 도중**(대기 중/재시도 호출 중) 소진되는
  경우 거기서 중단하고 마지막 조회 오류 전달, 종목 간 예산 누적,
  읽기 재시도 중 주문·알림·노션 쓰기 미발생, `run_auto_sell()`의 잔고
  복구 후 판정 지속 및 최종 실패 시 미주문.
  **여기까지는 전부 mock 기반이다** — 실제 KIS 응답·지연·장애를 겪은
  것이 아니라, 그런 상황을 가정했을 때의 코드 분기만 확인한 것이다.
- **검증 B — 실제 자연 실행(미완료, 운영 미확인)**: 실 KIS 연동에서
  재시도가 실제로 복구를 만들어내는지, 재시도가 붙은 회차의 실행 시간이
  워크플로 제한 안에 드는지는 **아직 확인되지 않았다**. 다음 장중
  자연 실행 로그(`재시도 … 성공` / `재시도 … 후에도 실패` /
  `재시도 예산 소진` 로그와 단계 소요 시간)로 별도 판단해야 한다.
  이번 작업에서 시험 호출·수동 실행으로 확인하지 않았다.
- **운영 미반영**: 병합·배포·워크플로 수동 실행·실제 API 시험 호출·주문·
  외부 메시지·노션 데이터 변경 모두 하지 않았다. `MAX_ORDERS_PER_DAY = 0`
  (신규매수 운영 보류)과 추가매수 설정은 변경 없다.
- 범위 밖으로 둔 것: 노션 주문 이력 조회 오류, KRX 로그인 메시지
  (자격증명 미설정 — 장애 아님), 전략·매수 예산·부분청산 회계.

### 후속 보완 — 2026-09-16 (f3c61b5 검토)

- **재시도 예산 검사 위치**: 기존 구현은 남은 예산을 재시도 묶음을
  시작할 때 한 번만 봤다 — 남은 예산이 0.5초여도 1초를 자고 다음 호출을
  걸었다. 이제 매 대기 직전과 대기 뒤 호출 직전에 `_read_retry_budget_left()`
  로 다시 확인하고(진행 중인 묶음의 경과 시간도 함께 뺀다), 소진되면
  마지막 조회 오류를 그대로 올려 **기존 보류 경로를 그대로 탄다**.
  재시도 대상·횟수·대기 간격·적용 지점은 그대로다.
- **"추가 소요 최대 83초 보장" 철회**: 위 예산 항목 참조. `requests`의
  `timeout`은 전체 실행시간의 엄밀한 상한이 아니다.
- **테스트 격리 수정**: 기존 픽스처는 "실제 `.env` 로딩을 막았다"고
  적었지만, `kis_client`는 import 시점에 `load_dotenv()`를 부르고 그
  import는 픽스처보다 먼저 일어난다 — 픽스처로는 로딩 자체를 막을 수
  없다. 주장을 정정하고, 대신 **로딩된 결과를 지운다**:
  `tests/helpers.py`에 공용 헬퍼를 추가해
  (`scrub_credential_env()` — `.env.example`의 키를 전부 환경에서 제거,
  `block_external_http()` — `requests`의 전 메서드 + `Session.request`
  차단) 이 테스트 파일에서 재사용한다. 필요한 조회 응답만 개별
  테스트가 `requests.get`을 mock해 열어주고, 자격증명 값은 읽지도
  출력하지도 않는다(격리 자체를 확인하는 테스트 2건 추가).
- **근거 표현 정정**: "KIS 서버 측 장애 확정" → 일시적인 서버·네트워크
  문제일 **가능성**. "재시도 한 번으로 복구 가능했음" → 후속 조회
  성공은 재시도가 도움이 될 가능성을 시사할 뿐, 실패한 그 호출의
  복구는 **입증되지 않음**. mock 검증(A)과 실제 자연 실행 검증(B)을
  분리해 기록.
- 이번 보완에서도 주문·정정·취소·노션 쓰기·메시지 경로에는 재시도를
  넣지 않았고, 매매 설정·체결 확인·중복 방지 로직은 건드리지 않았다.
  병합·배포·워크플로 실행·실제 API 시험 호출도 하지 않았다.

## main 병합 승인 — 2026-09-15

- 사용자가 PR #4의 main 병합을 명시적으로 승인했다. Draft를 해제하고 GitHub의 일반 병합 절차로 진행한다. 보호 규칙을 우회하지 않는다.
- 병합 직전 05:46:12 KST 읽기 전용 재조회: 미확인 모의 매도 **0건**. 데이터 연결/이관은 수행하지 않는다.
- 검증된 코드의 테스트 결과는 127 passed이며 이후 변경은 문서뿐이다. 신규매수 상한 0 및 주문/알림/워크플로 수동 실행 금지를 유지한다.
- 이 항목은 병합 요청 직전 기록이다. 실제 병합 완료 여부와 병합 커밋은 PR #4의 GitHub 상태를 기준으로 확인한다. 자연 실행의 실제 연동 검증은 아직 완료되지 않았다.

## 최종 검토 재확인 — 2026-09-15

- 검토 head `3dbf33d`: PR #4 OPEN / Draft / MERGEABLE. 등록된 CI 검사와 승인 리뷰는 없으므로 MERGEABLE을 안전성 승인으로 해석하지 않는다.
- 전체 로컬 테스트를 네트워크와 `.env` 로딩을 차단하여 재실행: **127 passed (16.34초)**. `git diff --check` 통과.
- 관계 대상 검증, 미연결 주문 보류, 청산된 포지션의 주문·일지 증거 대조 경로를 재확인했다. 이번 점검 범위에서 추가 수정 사항은 찾지 못했다. 실제 증권사 연동 검증을 대신하지 않는다.
- 05:42:24 KST 미확인 모의 매도 재조회 **0건**. 관계 연결·데이터 이관 불필요. 증권사 주문 상태는 조회하지 않았다.
- 병합·배포는 미실행. 다음 단계는 사용자의 명시적 main 병합 승인이다. 신규매수 상한 0이어도 자동매도/추가매수는 별도 경로로 실행될 수 있으므로 병합은 운영 동작에 영향을 준다.

## 기존 주문 연결 점검 완료 — 2026-09-15

- 05:37:28 KST, Notion에서 모의계좌·매도·상태 `주문중/실패`를 읽기 전용 조회한 결과 **0건**. 기존 연결 후보 및 데이터 이관 대상이 없다.
- 상세 범위·근거·재확인 절차: [POSITION_ORDER_LINK_AUDIT.md](POSITION_ORDER_LINK_AUDIT.md).
- 주문/보유/일지 데이터와 스키마는 이번 작업에서 변경하지 않았다. 주문·알림·수동 워크플로·병합·배포 미실행, 신규매수 상한 0 유지.
- 다음 단계: Draft PR #4 최종 검토 및 병합 승인. 배포 직전 미확인 주문을 다시 조회하며, 새 이관 대상이 생기면 후보 검토와 별도 연결 승인이 선행되어야 한다.
- 이번 변경은 문서만 수정했으며, 직전 코드 검증 결과는 `a24f2b6`의 127개 테스트 통과다. 아래 내용은 날짜별 과거 이력이다.

## 포지션 관계 속성 적용 완료 — 2026-09-15

- 승인된 Notion 스키마 변경을 적용했다: 자동주문 DB(`NOTION_ORDERS_DB_ID`)에 `보유종목` 단방향 relation을 추가하고 보유종목 DB(`NOTION_DB_ID`)를 대상으로 지정했다.
- 적용 직후 두 DB 스키마를 다시 읽어 relation 유형·대상·기존 속성 보존을 확인했다. 결과 `added_and_verified`; 기존 주문·보유 레코드는 변경하지 않았다.
- 코드 변경: 신규 매도 사전 기록에 포지션 page ID를 필수로 연결, 관계 없는 기존 미확인 주문은 날짜로 추정하지 않고 보류, 현재 보유 목록에 없는 청산 포지션은 연결된 page·주문·일지 증거를 검증한 뒤 주문 상태만 복구한다.
- 수량·계좌·종목·보유 상태 불일치, 관계 누락/복수 관계, 체결 조회 오류는 주문 전송 또는 청산 확정을 막는다. 신규매수도 미확인 매도 종목을 보류한다.
- 로컬 테스트 최종 **127 passed**, `py_compile`(venv Python), `git diff --check` 통과. 외부 주문·카카오 알림·Notion 레코드 변경·워크플로 실행은 하지 않았다.
- 남은 작업: 기존 relation 없는 미확인 매도 주문의 후보 연결표를 읽기 전용으로 만들고, 각 주문 연결은 별도 승인 후 수행한다. 과거 성공 주문은 이관하지 않는다.

## 포지션 연결 설계 — 2026-09-14

- [POSITION_ORDER_LINK_PLAN.md](POSITION_ORDER_LINK_PLAN.md)에 두 구현 비교, relation 변경 요청, 기존 주문 이관, 이미 청산된 포지션 복구, 필수 테스트를 정리했다.
- 기반은 PR #4 `c8bb33a`. 별도 브랜치 `8aee408`의 자동 만료/재매도 정책은 도입하지 않는다.
- 상태: 설계 완료. 코드 구현·Notion 스키마 변경·데이터 이관·배포 미실행. 외부 변경 승인 범위는 자동주문 DB의 단방향 `보유종목` relation 1개 추가이며, 기존 레코드 연결은 별도 승인 대상이다.

## 배포 전 재검토 — 2026-09-14 (Codex)

- PR #4: https://github.com/uri2ri/stock-monitor/pull/4 — OPEN / Draft. 점검 시작 head `a174a8b`, 자동 검사 결과 없음. 최종 head는 PR에서 확인한다.
- 최신 원격 main `205d41c`와 기준 `276eee6`의 차이는 데이터 파일 4개뿐이며 운영 코드 변경 없음. main 병합 없이 수정 브랜치에서 검증했다.
- 기존 109개 테스트 재실행 통과. 추가로 청산 일지 수량 오류와 체결 응답 내역 누락을 수정하고 회귀 테스트 4개를 추가했다. 최종 **113 passed (16.07초)**, 네트워크와 `.env` 로딩 차단. `git diff --check` 통과.
- 일지 수량: 노션 보유수량 200주, 확인 체결수량 100주인 경우 이전 코드는 200주를 기록했다. 이제 확인한 `qty=100`을 기록한다. 과거 부분매도분까지 합산하는 포지션 회계는 이번 수정 범위 밖이다.
- 응답 내역: `rt_cd=0`이어도 `output1`이 없거나 null/객체이면 정상 미체결로 판단하지 않고 오류로 올린다.
- 별도 원격 브랜치 `fix/auto-sell-recovery-review` (`8aee408`)도 매도 복구를 변경하며 현재 열린 PR은 없다. PR #4와 중복되므로 별도 확인 없이 함께 병합하지 않는다.
- 9월 11일 확인한 secret/Notion 읽기 접근/YAML 검증은 설정 변경이 없어 반복하지 않았다. 주문·알림·Notion 변경·워크플로 실행·병합·배포는 하지 않았다. 신규매수 상한 0 유지.
- 남은 운영 판단: 같은 날 재진입 및 날짜 불명일 때 청산이 차단될 수 있다. 이는 자동매도 기회를 놓칠 수 있는 한계이므로 무조건 병합 가능으로 판단하지 않는다. 취소/잔여 주문 재전송이나 스키마 변경은 별도 승인 범위다.
- 병합 후 검증 절차: 승인된 병합 뒤 자연 실행된 모의투자 watch run의 head SHA/오류 로그/상한 0 로그 확인 → 실제 매도 발생 시 접수 `주문중`, 미체결 보유 유지, 전량 체결·잔고 0 후 일지/청산/성공 순서 확인. 조건 미발생은 미검증으로 남기며, 테스트를 위해 주문이나 workflow_dispatch를 만들지 않는다.

## 후속 재검토 — 2026-09-11 (Claude, bec7cac 이후)

앞선 독립 검토(`bec7cac`)의 수정 자체를 다시 재추적해 2건을 더 찾아 고쳤다(109 passed, 기존 103 + 6). 상세는 [매도 체결 확인 설계 및 한계 - "후속 재검토에서 발견·수정한 문제"](SELL_CONFIRMATION.md)를 참고.

- **신규매수 경로가 매도 미확인/기존 보유 종목에 다시 진입할 수 있는 문제 (심각)**: `select_buy_candidates()`가 "이미 이 종목을 보유 중인가"를 전혀 확인하지 않아, 상관군 캡의 "이 종목 기존 유닛은 0" 전제가 깨진 채 통과할 수 있었고 병합 시 `run_auto_pyramid`의 유닛 캡을 우회했다. 원 작업 지시의 "신규매수 경로도 충돌하지 않는지" 요건이 실제로는 구현되지 않았던 것 - `select_buy_candidates`에 보유 여부 확인 게이트를 추가해 고쳤다.
- **미확인 매도가 없는 종목에도 매 회차 노션 매수일 조회가 추가된 문제 (경미)**: `_split_stale_pending_sells()`가 빈 pending 리스트에도 조회를 했다 - 빈 리스트면 조회 자체를 하지 않도록 고쳤다.
- **명시적으로 남긴 한계**: 매수일·주문일 비교가 날짜 단위라 같은 날 재진입은 구분하지 못한다(안전한 쪽으로 차단). 시각 단위로 정밀화하려면 "매수일"을 "매수일시"로 바꾸는 스키마 변경이 필요해 범위 밖으로 남겼다.
- 이번 재검토도 로컬 코드 수정·테스트만 수행 - 실주문·모의주문 전송, 카카오 알림, 노션 데이터/스키마 변경, 워크플로 수동 실행 없음. GitHub secret·Notion 접근은 지난 검토(아래)에서 이미 확인했고 설정 변경·실패 근거가 없어 이번엔 재조회하지 않음.

## 독립 검토 — 2026-09-11 (Claude)

Codex 작업(`592fe72`, `15bafb8`, `42f4a6d`)을 `276eee6` 대비 diff로 독립 재검토했다. 기존 테스트 96개 통과를 안전의 근거로 보지 않고 코드를 직접 재추적해, 재진입 종목 오매칭으로 자동매도/추가매수가 영구 차단될 수 있는 문제(심각)와 체결 조회 오류 은폐 문제(경미) 2건을 새로 찾아 수정하고 회귀 테스트 7건을 추가했다(103 passed). 상세 원인·수정·근거는 [매도 체결 확인 설계 및 한계 - "독립 검토에서 발견·수정한 문제"](SELL_CONFIRMATION.md)를 참고.

- 신규매수 상한 `MAX_ORDERS_PER_DAY=0` 그대로 유지. 실주문·모의주문 전송, 카카오 알림, 노션 데이터 변경, 워크플로 수동 실행 없이 로컬 코드 검토·테스트만 수행.
- GitHub secret `NOTION_LEDGER_DB_ID` 존재를 `gh secret list`로(값 노출 없이) 확인. Notion 통합의 해당 DB(매매일지) 읽기 접근을 실제 토큰으로 GET 요청 1회 실행해(읽기전용) 확인 - 응답에서 DB 제목과 스키마만 확인했고 레코드는 조회·수정하지 않음.
- 워크플로 YAML 파서 검증(`auto-trade.yml`) 완료 - 임시 환경에 `pyyaml` 설치 후 파싱, 문법 오류 없음.

### 다음 작업 (배포 전 확인 필요)

- 이 브랜치의 Draft PR(#4)을 사람이 리뷰 후 병합. 새 커밋은 기존 PR에 반영(중복 PR 생성 안 함).
- 병합 후 신규매수 재개(`MAX_ORDERS_PER_DAY` 0→3)는 이번 검토 범위 밖 - 실거래 점검 결과를 보고 사람이 결정.
- "재진입 종목 오매칭" 수정은 Notion 스키마 변경 없이 매수일 비교로 완화한 것이라, 근본 해결(주문 DB에 보유종목 relation 추가)은 별도 스키마 변경 작업으로 남아 있음.
- 같은 날 재진입의 날짜 단위 비교 한계(위 "후속 재검토" 참고)도 근본 해결하려면 "매수일" 칸을 "매수일시"로 바꾸는 스키마 변경이 필요 - 지금은 범위 밖으로 남겨뒀다.

## 최신 작업 이력 — 2026-09-11 (Codex)

- 작업 브랜치: `codex/buy-budget-revalidation`
- 기준 코드: `276eee6` (점검 당시 `origin/main`)
- 상태: 아래 수정은 로컬 커밋 완료, 원격 push·PR·병합·운영 반영은 미실행.
- 신규매수 상한: `MAX_ORDERS_PER_DAY=0` 유지. 주문·카카오 알림·노션 데이터 변경 없이 검증.

| 커밋 | 변경 사항 | 검증 |
| --- | --- | --- |
| `592fe72` | 신규 후보별 예약 현금에 비용 포함, 재조회한 최신가에도 150,000원 상한 적용 | 전체 78개 통과 |
| `15bafb8` | 매도 접수와 체결 분리, 다음 회차 체결 확인, 매도 미확인 종목 추가매수 차단, 청산 일지 중복 방지, 워크플로 일지 DB 설정 연결 | 전체 96개 통과 |

### 점검에서 재현한 문제와 수정 결과

- 현금 2,003,000원에 비용 포함 2,005,000원 후보 두 건이 통과하던 문제: 앞 후보의 비용까지 예약 차감해 두 번째 후보 차단.
- 최초 150,000원 → 최신가 150,100원에서도 통과하던 문제: 최신가 가격 상한 재검사로 차단. 정확히 150,000원인 경계값은 허용.
- 매도 접수만으로 보유를 청산 처리하던 문제: 접수는 `주문중` 유지, 주문 전체 체결과 증권사 잔고 0 확인 후 일지 → 보유 청산 → 주문 `성공` 순으로 처리.
- 미체결·부분체결·조회 실패 시 보유 유지 및 재매도 방지. 미확인 매도가 있으면 해당 종목 추가매수도 보류.
- `auto-trade.yml`에 기존 `NOTION_LEDGER_DB_ID` secret 참조 추가. 실제 secret 존재/DB 접근 권한은 미확인.

### 검증 범위와 다음 작업

- 외부 네트워크와 로컬 `.env` 로딩을 차단한 합성 데이터·대체 함수 기반 검증: **96 passed** (독립 검토 이후 **103 passed**, 후속 재검토 이후 **109 passed** - 위 항목 참고). 모두 모의 검증이며 실제 증권사 연동 검증은 아니다.
- 신규매수 보류, 비용 예약, 가격 상한, 청산 조건, 추가매수 조건, 미체결/부분체결/다음 회차 전량 체결, 전날 주문 조회, 조회 장애, 일지 중복 방지 포함.
- `git diff --check` 통과. 워크플로 YAML 파서 검사는 로컬 `yaml` 패키지 부재로 미실행 → 독립 검토에서 실행 완료(문법 오류 없음).
- 다음: 변경 검토(→ 독립 검토 완료) → 일지 DB secret/접근 권한 확인(→ 완료) → 운영 반영 → 실제 모의투자 연동 검증. 신규매수 재개는 그 이후 별도 결정.
- 주문번호 불명·전날 미완료·복수 미확인 매도·체결 후 잔여 보유는 수동 확인 대상. 자동 취소/재전송은 구현하지 않음.
- 과거 `성공` 주문의 소급 복구는 하지 않음. 보유 청산 후 주문 상태 갱신만 실패한 경우 주문 기록 수동 확인 필요.
- 상세: [매도 체결 확인 설계 및 한계](SELL_CONFIRMATION.md).

### 병합 후 운영 확인 이력 (사용자 제공)

- PR #3 병합 커밋 `ba27ff7c`, 이후 실행 run #985 (`34553748354`, `99c16ff`) 성공.
- 신규매수 보류 로그, 후보별 반복 거절 카톡 미발송, 보유 조회·주문 기록 기반 재시도 로그 확인.
- 해당 회차 청산 조건 충족 종목 없음. 실제 청산 체결까지 검증한 실행은 아님.
- 확인용 1회성 예약은 실행 완료로 전달받음. 이번 로컬 작업에서 재조회하지 않음.

아래 내용은 이전 작업의 배경 이력이다. 최신 로컬 수정/검증 상태는 위 항목을 기준으로 한다.

## 현재 구현 상태
- 종목 스캔 완료 (`scan_all.py`, `high20`/`high20_next` 둘 다 저장)
- 장중 돌파 감시 완료 (`intraday_watch.py`)
- KIS 모의투자 주문 연결 완료 (`kis_client.py`)
- 장중 자동매수 파이프라인 fail-closed 강화 완료 — PR #2 **병합 완료**
  (`main`, 2026-09-10 08:28 UTC)
  1. 구버전 스캔 CSV의 신규매수 차단 (화면 표시는 계속, 자동매수만 보류)
  2. 후보 선정(워치리스트 순위)과 판정(`judge()`) 기준을 `high20_next`로 통일
  3. KIS 최신 가격 검증 실패 시 신규매수 보류 (fail-open → fail-closed)
  4. 휴장일을 반영한 스캔 신선도 검사 (거래일 달력 기반)
  5. 주문 상태별 재시도 구분·중복 방지 — 기존 설계가 이미 요건 충족 확인(코드 변경 없음)

## 최근 중요 결정
- `high20_next`(당일 포함 20일 고가)를 장중 진입 판정 기준으로 사용,
  없거나 무효(NaN·inf·0 이하)면 `high20`으로 대체하지 않고 보류
  (`_is_valid_threshold()`, `intraday_watch.py`)
- 워치리스트 후보 선정도 `dist_atr`(당일 제외 기준) 대신 `dist_atr_next`로
  재계산 — `judge()`의 판정 기준과 후보 우선순위를 일치시킴. 화면 표시용
  `high20`/`status`/`dist_atr`와 구버전 CSV 자체는 그대로 읽힘
- KIS 주문 직전 최신가 재검증을 fail-open → fail-closed로 전환 (조회
  실패·무효 가격이면 신규매수 보류, 판정 시점 가격으로 되돌아가지 않음).
  시장경보 필드 조회 실패의 기존 fail-open 정책은 별개로 유지
- 최종 현금 여력 확인에 `ORDER_COST_RATE`(0.25%, `backtest.py`의
  `COST_RATE`와 동일값) 포함 — 이 재검증에만 적용, 기존 게이트5는 미변경
- 야간 스캔 신선도 검사를 주말만 건너뛰는 로컬 계산에서
  `screener.trading_days()`(scan_all.py가 이미 쓰는 KRX 거래일 달력)
  기반으로 교체. 달력 조회 실패 시 "평일이니 어제"로 추정하지 않고
  "확인 불가"로 보고 신규 진입 보류(청산·추가매수는 계속 작동)
- `alerted`는 카카오 알림 여부만 의미
- 주문 여부는 `has_order_today()`로 별도 확인

### 매수 주문 상태별 재시도 표

매도는 위 최신 작업에서 접수(`주문중`)와 확인 완료(`성공`)를 분리했다.

| 상황 | 노션 기록 상태 | `has_order_today` 차단 | 재시도 정책 |
|---|---|---|---|
| 전송 "전" 조회 실패 | (기록 없음) | 해당 없음 | fail-closed로 이번 회차만 보류, 다음 회차 재평가 |
| 명확한 거부(`rt_cd != "0"`) | 거부 | 아니오 | 재시도 허용, `MAX_REJECTIONS_PER_STOCK`(3회) 넘으면 중단 |
| 응답 시간초과 등 접수 여부 불명 | 실패 | 예 | 재전송 금지 |
| 접수·미체결/부분체결/완전체결 | 성공 | 예 | 중복 진입 금지 (세 경우를 "성공" 하나로 취급) |
| 사전기록 후 유령 행("주문중") | 주문중 | 예 | 자동 정리 안 함, 시작 시 경고만, 사람이 정리 |
| 취소 주문 | 해당 없음 | — | 취소 API 자체가 없어 해당 없음(범위 밖) |

### 달력 제공 방식 / 구버전 CSV 전환
- 제공자: `screener.trading_days(end, count)` (pykrx 기준 종목 시세 인덱스,
  scan_all.py 자체 스캔이 이미 사용 중). 매 호출 실시간 조회라 별도 캐시
  갱신 불필요, 실패 시 `KrxUnavailable`
- 구버전 CSV 전환: 별도 마이그레이션 불필요 — 야간 스캔(`scan_all.py`)을
  한 번 더 돌리면 `high20_next`가 채워진 새 `scan_latest.csv`로 자동 전환.
  재스캔 전까지 화면 표시는 계속되지만 신규매수만 보류

## 운영 상태 (중요)
- PR #2 병합 직전(2026-09-10 08:27 UTC) `MAX_ORDERS_PER_DAY`를 3 → **0**
  으로 내려 **신규 자동매수를 운영 보류 중**(커밋 `b3adc7f`). 병합 후
  변경 사항을 실거래에서 점검하기 위한 조치이며, `select_buy_candidates()`
  의 신규(ORDER_NEW) 전용 예산만 낮춘 것이라 추가매수(`MAX_PYRAMID_ORDERS_PER_DAY`)
  ·청산(`run_auto_sell`)에는 영향 없음. **재개 시 사람이 3으로 되돌려야 함**
  (자동 복구 아님) — 점검 결과를 보고 결정.
- 운영 보류 중 발견된 부작용 수정 완료(2026-09-11): `MAX_ORDERS_PER_DAY=0`
  이어도 `select_buy_candidates()`가 후보별 가격·유닛금액·상관군·현금
  검사를 그대로 거쳐 후보마다 거절 카톡을 반복 발송하던 문제. 이제 상한이
  0이면 계좌 조회·후보별 검사·거절 알림 없이 바로 빈 목록을 반환하고
  로그(`신규매수 운영 보류: 일일 상한 0`)만 남김. 상한이 정상값(예: 3)이고
  그날 소진돼 `remaining_slots == 0`이 된 경우는 대상이 아니며 기존
  후보별 검사·"우선순위 밀림" 알림 그대로 유지. 자동매도·추가매수·주문
  기록 기반 재시도 정책은 영향 없음(별도 캡·경로 사용). 상한값 자체는
  여전히 0 — 재개는 위 항목대로 사람이 결정.

## 현재 문제
- 보유종목 검색 오류 확인 필요
- (위 운영 보류 참고) 신규 자동매수가 `MAX_ORDERS_PER_DAY=0`으로 막혀
  있는 동안은 정상 동작이 아니라 의도된 일시 정지 상태임

## 이전 테스트 이력 (68개 기준)
- `python -m pytest tests/ -q` → **68 passed, 0 failed** (커밋 `dfd967e` 기준, PR #3 최종 diff와 동일)
- 신규 `tests/test_zero_cap_holds_new_buy.py` 5건은 모두 통과
  (상한 0 조기반환·로그, 상한 3 기존 흐름 유지, 추가매수·자동매도 무영향)
- `MAX_ORDERS_PER_DAY=3→0`(운영 보류) 전환 이후 이 값을 monkeypatch하지
  않아 실패하던 기존 6건(`test_multi_candidate_reservation.py` 2건,
  `test_order_state_retry_policy.py` 3건, `test_bug3_final_price_reverify.py`
  1건)은 모두 정상 신규매수 통과·재검증·현금 예약 동작을 검증하는
  테스트로 확인 — 검증 목적·assert는 그대로 두고 테스트 내부에서
  `MAX_ORDERS_PER_DAY`를 3으로 monkeypatch해 운영 보류 설정과 분리함
  (프로덕션 상한은 여전히 0, 변경 없음)

## 남은 한계 / 후속 확인
- 최종 가격 재검증은 시장가 주문의 실제 체결가격을 보장하지 않음
  ("주문을 낼지 말지"만 확인)
- `ORDER_COST_RATE`는 `backtest.py` 근사치 재사용 — 실제 매수 수수료와
  정확히는 다를 수 있음(매수는 세금 없어 다소 과대추정, 안전한 방향)
- "취소된 주문" 상태는 취소 API가 없어 다루지 않음(범위 밖)
- 원격 환경에서 synthetic 데이터로만 검증 — 실 배포 전 실제
  `scan_latest.csv`(구버전·최신 각각)로 `load_watchlist()`/`judge()` 재확인,
  실 계좌 규모에서 비용 포함 현금 게이트가 기존 게이트5와 크게 어긋나지
  않는지 점검 권장
- `MAX_ORDERS_PER_DAY=0` 운영 보류 해제(3으로 복원) 여부는 실거래 점검
  결과를 보고 사람이 결정해야 함 (자동 복구 없음)

## 이전 정상 기준
- commit: `5b80f4d` (`main`, PR #2 병합 `d2f29e1` + 운영 보류 `b3adc7f` +
  야간 스캔 `5b80f4d` 이후 최신)
- PR: https://github.com/uri2ri/stock-monitor/pull/2 (병합됨, `main`)

## PR #3 작성 당시 이력 (이후 `ba27ff7c`로 병합)
- 브랜치: `claude/holdings-search-error-kfynqa`, base `main`
- https://github.com/uri2ri/stock-monitor/pull/3
- `main`(당시 최신 `cb14d02`) 대비 diff: 6 files, +184/-8
  - `kis_client.py`(최소 수정 8줄) — 신규매수 운영 보류(상한 0)에서 후보별
    거절 카톡 반복 발송 수정
  - `tests/test_zero_cap_holds_new_buy.py`(신규 5건)
  - `test_multi_candidate_reservation.py`·`test_order_state_retry_policy.py`·
    `test_bug3_final_price_reverify.py` 기존 6건에 `MAX_ORDERS_PER_DAY=3`
    monkeypatch 추가(총 18줄) — 운영 보류 설정(0) 의존 제거, 검증 목적 유지
  - `PROJECT_STATUS.md`(문서, 최종 테스트 결과 반영)
- 프로덕션 상한은 `kis_client.py:108` `MAX_ORDERS_PER_DAY = 0` 그대로 유지
- 당시 상태: Draft, 미병합. 이후 병합 및 실행 확인은 위 최신 이력 참고.
