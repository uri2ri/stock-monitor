# 돌파 후 미진입 추적 운영 적용 점검

이 문서는 PR #6의 운영 적용 전 점검 기록이다. 2026-09-21 사용자 승인으로
주식 페이지 아래 별도 추적 DB를 생성했고 37개 속성의 타입·선택값과 빈 DB 조회를
검증했다. GitHub 저장소 시크릿과 로컬 .env·Streamlit 설정에 DB ID를 등록했다.
인증정보 값은 출력하거나 커밋하지 않았다. 기존 보유·주문 DB는 변경하지 않았다.
배포된 Streamlit Cloud 설정도 기존 Secrets 보존 및 저장 후 재조회로 확인했다.
병합·배포·운영 워크플로 실행은 하지 않았다. 연결 설정 완료를 운영 성공으로 해석하지 않는다.

## 확인된 경로

| 단계 | 구현 | 확인 범위 |
| --- | --- | --- |
| 활성화 | `breakout_tracker.is_enabled()` | 토큰과 추적 DB ID의 존재 검사. 권한·스키마 검증은 아님 |
| 최초 관측 | auto-trade → 로컬 outbox → prepare → checkpoint → deliver | 동일 종목 순서와 최초 값 보존 |
| 생성 복구 | queued → 원격 creating 저장 → POST | 로컬 bare remote와 새 clone으로 push 거절·응답 유실·중복 차단 검증 |
| 주문 후속 확인 | `breakout_reconcile` | 회차당 5건/60초 시작 예산. 주문 식별 증빙 확인 후 추적 DB 갱신 |
| 일별 가격 | nightly-scan → `breakout_track_update.py` | 확정 종가 갱신. 최초 값 유지. 단계 실패는 continue-on-error이므로 개별 로그 확인 필요 |
| 조회 화면 | 앱의 돌파 후 미진입 탭 | 읽기 전용. 설정 없음과 조회 실패를 구분 |

관련 회귀 테스트와 네트워크 차단 전체 테스트 327개 통과(코드 `14cc755`).
GitHub CI 통과를 의미하지 않는다. PR은 마지막 확인 시 초안이며 등록된 CI가 없었다.

## 적용 전에 실제로 확인할 항목

1. 별도 추적 DB의 필드 이름·타입·선택값을 `BREAKOUT_TRACKING.md` 및
   `notion_repo.py`의 BT 상수와 대조한다. 기존 보유·주문·일지 DB를 재사용하지 않는다.
2. GitHub Actions와 웹앱의 토큰·추적 DB 설정 및 통합 접근 권한을 각각 확인한다.
   토큰 값은 보고서·로그에 출력하지 않는다.
3. 단일 auto-trade writer, 브랜치 push 권한, artifact 보존 설정을 확인한다.
   보호 규칙이 체크포인트 push를 막으면 생성도 보류된다.
4. 병합·배포·운영 실행 범위가 별도로 승인된 뒤 적용한다. auto-trade 실행은
   주문·알림 경로도 포함하므로 관측만 점검하려고 임의 실행하지 않는다.
5. 적용 후 최초 이벤트와 원격 체크포인트, Notion 관측ID, 앱의 최초 값·가격기준일을
   대조한다. 야간 갱신 실패와 보류 건수 증가도 확인한다.

## 과거 보류 항목 확인 절차

원격 저널과 해당 회차 artifact를 먼저 보존하고 이벤트 ID, 종목, 최초 시각,
계획 상태, owner와 실패 단계를 대조한다. 운영 원본을 덮어쓰거나 상태를
자동 변환하지 않는다.

- `queued`: 새 프로토콜의 미시도 항목. 정상 순환 처리 대상이다.
- `creating`/`ambiguous`: 관측ID 조회 결과가 종목·최초 시각과 일치하는지 확인한다.
  조회 0건은 생성 실패 증거가 아니므로 재POST하지 않는다.
- 다른 owner의 과거 `prepared`: 이전 실행의 POST 여부를 입증할 수 없으면 보류를 유지한다.
- 관측 대상 없는 이벤트: 최초 관측과 보유 근거를 확인한다. 가상의 최초 값으로 채우지 않는다.

증빙이 확보된 개별 항목의 수동 정리는 별도 검토·승인 대상으로 남긴다.
현재 운영 보류 항목의 존재 여부와 건수는 조회하지 않았으므로 미확인이다.
