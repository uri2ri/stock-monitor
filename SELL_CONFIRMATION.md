# 매도 체결 확인 변경

신규매수 상한은 0을 유지한다. 매도 접수는 `주문중`으로 남기고, 주문 전체 체결과 증권사 잔고 0을 확인한 뒤 일지 생성 → 보유 청산 → 주문 성공 순서로 기록한다.

다음 회차에는 미확인 매도 기록을 날짜 제한 없이 조회한다. 부분체결·미체결·조회 오류에서는 보유를 유지하고 해당 종목의 재매도 및 추가매수를 보류한다. 주문번호 불명, 전날 미완료 주문, 여러 미확인 매도, 체결 후 잔여 보유는 확인 알림 대상이다. 취소나 잔여 주문 재전송은 자동으로 수행하지 않는다.

일지를 만든 뒤 청산 상태 갱신이 실패하면 보유종목 관계로 기존 일지를 확인해 중복 생성을 방지한다. 청산 처리 후 최종 주문 상태 갱신만 실패한 경우에는 알림에 따라 주문 기록을 수동 확인해야 한다. 배포 전 이미 `성공`으로 기록된 과거 주문을 소급 복구하지 않는다.

자동매매 워크플로에 기존 `NOTION_LEDGER_DB_ID` secret 연결을 추가했다. secret 존재와 Notion 통합의 일지 DB 읽기 접근은 2026-09-11 독립 검토에서 값 노출 없이 직접 확인했다 (아래 "독립 검토에서 발견·수정한 문제" 참고). 외부 계정이나 주문 기록은 이번 검증에서 변경하지 않았다.

검증: 외부 네트워크와 로컬 .env 로딩을 차단한 전체 테스트 103개 통과(기존 96 + 독립 검토에서 추가한 7건). 미체결, 부분체결, 다음 회차 전량 체결, 전날 주문 조회, 잔고/체결 조회 실패, 추가매수 차단, 중복 일지 방지, 재진입 종목 오매칭 방지, 체결 조회 오류 노출 등을 포함한다. 실제 증권사 API 통합 검증은 별도다.

## 독립 검토에서 발견·수정한 문제 (2026-09-11)

원 커밋(`592fe72`, `15bafb8`)의 테스트 96개 통과만으로는 안전을 보장하지 못한다고 보고 코드를 직접 재추적해 아래 2건을 찾아 고쳤다. 커밋 `276eee6` 대비 diff, KIS 응답 코드·`tr_cont` 연속조회 규약, Notion 스키마(주문 DB에 보유종목 relation 없음, 읽기전용으로 실제 스키마 조회해 확인)를 근거로 판단했다.

1. **종목코드만으로 매칭해 재진입한 새 포지션을 영구 차단하는 문제 (심각)**
   - 자동주문 기록 DB에는 보유종목 점검표 행을 가리키는 relation이 없다(스키마 직접 조회로 확인). `fetch_unconfirmed_sell_orders`가 종목코드로만 매칭하므로, "청산 처리 후 최종 주문 상태 갱신만 실패"가 실제로 발생하면(위에서 이미 알려진 한계) 해당 종목의 "주문중" 기록이 노션에 영구히 남는다.
   - 문제는 그다음이다: 같은 종목을 나중에 재진입하면 점검표엔 **새 page_id의 새 행**이 생기는데(기존 행을 재사용하지 않음), `run_auto_sell`/`run_auto_pyramid`는 미확인 매도를 종목코드로만 대조해 이 새 포지션에 묶어버린다. 그 결과 새 포지션은:
     - `run_auto_sell`: 매 회차 `_reconcile_sell`이 "매도 체결 후 잔여 보유수량 확인" 오류를 반복 발생시키며(현재 보유 중이니 잔고가 있는 게 당연한데도) 정상 손절/추세청산 판정 자체가 실행되지 않는다.
     - `run_auto_pyramid`: 같은 이유로 추가매수 판정 자체가 실행되지 않는다.
     - 즉 사람이 수동으로 과거 주문 기록을 정리하기 전까지 **새 포지션이 손절선을 이탈해도 자동매도가 걸리지 않을 수 있다** - 한계 문서에 없던, 훨씬 심각한 결과다.
   - 수정: `_split_stale_pending_sells()`를 추가해, 미확인 매도 주문의 주문일시를 현재 포지션의 매수일(`notion_repo.fetch_holding_buy_date`, 기존에 매매일지 진입일 기록용으로 이미 쓰이던 함수 재사용)과 비교한다. 매수일보다 이전 주문이면 "과거 포지션 것"으로 분리해 현재 포지션의 매도·추가매수 판정을 막지 않고, 대신 별도 알림("재진입 이전 매도 기록 잔존 - 수동 확인/정리 필요")으로 사람이 정리하게 한다. 매수일을 확인할 수 없으면(조회 실패 포함) 기존처럼 안전한 쪽(차단)으로 남긴다 - 오판을 줄이는 대신 과잉차단을 택했다.
   - 회귀 테스트: `tests/test_sell_confirmation.py::test_stale_pending_sell_does_not_block_reentered_position`, `test_same_day_or_later_pending_sell_still_blocks_reentered_position`; `tests/test_restart_exit_pyramid.py::test_stale_pending_sell_does_not_block_pyramid_on_reentered_position`, `test_unresolvable_entry_date_still_blocks_pyramid`.
   - Notion DB 스키마를 바꾸지 않는 선에서의 완화책이다. 근본 해결(주문 DB에 보유종목 relation을 추가해 확실히 연결)은 스키마 변경이 필요해 이번 범위 밖으로 남긴다.

2. **체결 조회 API 오류 응답을 "미체결"과 구분하지 않는 문제 (경미)**
   - `get_order_execution`이 `rt_cd`(KIS 응답 코드)를 확인하지 않고 `output1`을 그대로 순회했다. 조회 자체가 실패한 응답(`rt_cd != '0'`)도 "해당 주문 없음(미체결)"과 똑같이 처리돼, 당일 중에는 알림 없이 다음 회차 재시도만 반복했다(포지션은 항상 유지되므로 안전하지만, 전날 미완료 검사 전까지 사람이 알 방법이 없었다).
   - `get_account_balance`에 이미 적용된 것과 같은 패턴으로 `rt_cd` 검증과 `tr_cont`(연속조회 필요) 검증을 추가해 즉시 예외로 올리도록 했다. `_reconcile_sell`의 기존 예외 처리가 그대로 알림을 보낸다.
   - 회귀 테스트: `tests/test_sell_confirmation.py::test_execution_query_error_raises_instead_of_silent_unfilled`, `test_execution_query_error_surfaces_same_day_notification`.

## 점검했지만 문제 없음으로 확인한 항목

- 신규매수 비용 포함 현금 예약(커밋 `592fe72`)과 최신가 150,000원 상한 재검사: `select_buy_candidates()`가 후보 선정 시점에 `fresh_unit_amount + fresh_cost`를 누적 차감하는 것, 150,000원은 통과·150,100원은 차단하는 경계를 코드 추적과 회귀 테스트로 재확인.
- `get_account_balance`의 `rt_cd`/`output1`/`output2`/`tr_cont` 검증: 오류·페이지 누락 시 빈 잔고로 오인하지 않고 예외로 올림을 확인. 연속조회(페이지네이션) 자체는 구현돼 있지 않아 발생 시 fail-closed로 처리를 보류한다 - 계좌 보유 종목 수가 적어 실무 영향은 낮다고 보지만, 보유 종목 수가 늘면 재확인이 필요하다.
- 매도 접수/체결 분리 후 매수 쪽 주문 상태-재시도 정책(`has_order_today`, 일일 상한 카운트)에 회귀 없음: 기존 `tests/test_order_state_retry_policy.py` 10건 그대로 통과.
- `has_ledger_for_holding()`을 이용한 일지 중복 생성 방지, `_record_ledger_after_sell` 실패 시 청산 보류: 실패 지점별(체결조회/잔고조회/일지기록/청산처리) 케이스를 코드 추적으로 재확인.
- `auto-trade.yml`의 `NOTION_LEDGER_DB_ID` 추가: YAML 파서로 문법 확인 완료(이전엔 로컬 패키지 부재로 미실행). GitHub secret 존재는 `gh secret list`로, Notion 접근은 실제 토큰으로 해당 DB를 읽기 전용(GET)으로 조회해 확인(제목 "매매일지 (청산 기록)", `보유종목` relation 속성 존재).

## 남은 한계 (변경 없음)

- 원인이 된 "청산 후 주문 상태 갱신 실패로 남는 유령 주문중 행" 자체는 여전히 자동 정리하지 않는다 - 위 1번 수정은 그 유령 행이 *다른(재진입한) 포지션*까지 막지 않게 할 뿐이다. 유령 행 자체는 여전히 사람이 정리해야 한다.
- 주문번호 불명·전날 미완료·복수 미확인 매도·체결 후 잔여 보유는 여전히 수동 확인 대상이다. 자동 취소/재전송은 구현하지 않았다.
- 과거에 이미 `성공`으로 기록된 매도 주문은 소급 복구하지 않는다.
