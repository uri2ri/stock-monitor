# 돌파 후 미진입 추적 — 설정과 동작

돌파 판정은 났는데 자금 부족·상한 등으로 매수하지 못한 종목의 흐름을 계속
볼 수 있게, **최초 관측 돌파**를 한 건씩 남기고 매일 확정 종가로 갱신한다.

이건 **관찰 기능이다.** 새 매수 규칙을 만들지 않는다. 진입·추가매수·청산
조건과 신규매수 상한은 이 기능과 무관하게 그대로다. 추적 기록은 주문을
만들지 않고, 보유수량·상관군 유닛·리스크 집계에도 들어가지 않는다.

> **운영 미반영.** 아래 Notion DB와 시크릿을 만들기 전까지 기능은 꺼져 있고
> (`is_enabled()` → False) 관련 코드 경로는 아무것도 하지 않는다. 이 문서의
> DB는 아직 만들지 않았다.

## 1. 켜는 방법

1. Notion에 **별도 DB**를 하나 만든다(아래 스키마). 보유종목 점검표·자동주문
   기록·매매일지에 칸을 추가하지 않는다 — 저 DB들은 실제 보유·주문의 근거라
   "사지 않은 종목"이 섞이면 그 자체로 오염이다.
2. 그 DB를 기존 Notion 통합(integration)에 공유한다.
3. GitHub 저장소 시크릿에 `NOTION_BREAKOUT_TRACK_DB_ID`를 넣는다.
4. Streamlit 웹앱에서도 보려면 `.streamlit/secrets.toml`에 같은 키를 넣는다.

키가 없으면 전부 비활성이다. 워크플로는 실패하지 않고 그냥 건너뛴다.

## 2. 환경변수

| 이름 | 쓰는 곳 | 없으면 |
| --- | --- | --- |
| `NOTION_BREAKOUT_TRACK_DB_ID` | auto-trade / daily / nightly-scan / 웹앱 | 기능 비활성 |
| `NOTION_TOKEN` | 위와 같음 (기존 시크릿 재사용) | 기능 비활성 |
| `BREAKOUT_TRACK_WRITER` | **auto-trade 워크플로에만** `auto-trade` | 추적 행을 만들거나 고치지 않는다(읽기는 그대로) |

`BREAKOUT_TRACK_WRITER`는 **쓰기 주체를 하나로 묶는 장치**다. Notion에는 원자적
유일키가 없어서 "조회 후 생성"만으로는 동시 실행에서 같은 돌파가 두 건으로
갈라질 수 있다. 그래서 생성·갱신은 이미 `concurrency` 그룹으로 직렬화돼 있는
auto-trade 워크플로 하나만 하도록 막았다. 다른 프로세스에서 기록을 시도하면
경고만 남기고 쓰지 않는다.

이건 **직렬화에 기댄 완화책이지 원자적 보장이 아니다.** 같은 워크플로를
강제로 병렬 실행하거나 생성 응답이 유실되면 중복이 생길 수 있다. 중복된
'추적중' 행이 발견되면 `find_active_breakout_track()`이 예외를 올려 자동
갱신을 멈추므로(임의로 한 건을 고르지 않는다) 사람이 정리해야 한다.

## 3. Notion DB 스키마

칸 이름은 `notion_repo.py`의 `BT_*` 상수와 정확히 일치해야 한다.

| 칸 이름 | 타입 | 채우는 주체 |
| --- | --- | --- |
| `종목명` | 제목(Title) | 최초 감지 |
| `코드` | 텍스트 | 최초 감지 |
| `최초감지` | 날짜(시간 포함) | 최초 감지 · 이후 불변 |
| `최초기준선` | 숫자 | 최초 감지 · 이후 불변 |
| `최초가격` | 숫자 | 최초 감지 · 이후 불변 |
| `최초ATR` | 숫자 | 최초 감지 · 이후 불변 |
| `추적상태` | 선택 — `추적중` / `종료` | 감지·종료 |
| `종료사유` | 선택 — `체결확인` / `사용자종료` | 종료 |
| `종료일` | 날짜 | 종료 |
| `자동매수결과` | 선택 — `미주문` / `자금부족` / `상한제한` / `상태미확인` / `주문접수` / `체결확인` | 주문 경로 |
| `미진입사유` | 텍스트 | 주문 경로(실행 결과 원문) |
| `결과기록` | 날짜(시간 포함) | 주문 경로 |
| `주문번호` | 텍스트 | 주문 접수 |
| `주문일` | 날짜 | 주문 접수 |
| `최신종가` | 숫자 | 야간 갱신 |
| `가격기준일` | 날짜 | 야간 갱신 |
| `최신ATR` | 숫자 | 야간 갱신 |
| `최신돌파선` | 숫자 | 야간 갱신 |
| `최초선대비%` | 숫자 | 야간 갱신 |
| `최초선대비ATR배수` | 숫자 | 야간 갱신 |
| `돌파선까지` | 숫자 | 야간 갱신 |
| `돌파선까지ATR배수` | 숫자 | 야간 갱신 |
| `기준선대비` | 선택 — `최초선위` / `최초선아래` / `확인불가` | 야간 갱신 |
| `새돌파` | 선택 — `해당` / `미해당` / `확인불가` | 야간 갱신 |
| `갱신상태` | 선택 — `갱신됨` / `갱신실패` | 야간 갱신 |
| `갱신메모` | 텍스트 | 야간 갱신 |
| `관망메모` | 텍스트 | **사용자만.** 코드는 절대 쓰지 않는다 |

`관망메모`는 `BT_READ_ONLY_PROPS`에 들어 있어 코드 경로에서 구조적으로
막혀 있다. "시스템이 막아서 못 산 것"(`미진입사유`)과 "내가 안 산
것"(`관망메모`)을 섞지 않기 위해서다.

## 4. 동작

### 최초 기록 (장중, `intraday_watch.py`)

돌파 판정이 난 시점에 종목당 활성 1건을 만든다. 기준선·가격·ATR 중 하나라도
유효한 양수가 아니면(없음·0·음수·NaN·inf·불리언) **기록 자체를 만들지 않는다** —
빈 칸을 0이나 추정치로 채우면 "최초 기준선 대비 %"가 통째로 거짓말이 된다.

- "최초"는 **이 프로그램이 처음 관측한 돌파**다. 시장에서의 최초 돌파가 아니다
  (감시는 10분 간격이고, 워치리스트에 든 종목만 보며, 조회가 실패한 회차는
  아예 보지 못한다). 화면 문구도 전부 "최초 감지"로 쓴다.
- 이미 활성 기록이 있으면 새로 만들지 않고 **최초 값을 그대로 둔다.** 며칠 뒤
  다시 돌파해도 그 추적 건의 "최초"는 처음 감지한 그 값이다.
- 조회 자체가 실패하면 만들지 않는다(fail-closed).
- 이미 보유 중인 종목은 만들지 않는다 — 추적이 체결로 닫힌 뒤 같은 종목이
  다시 감지돼도 "미진입"으로 되살아나지 않는다.

### 자동매수 결과

`kis_client.py`의 각 게이트가 **실제로 일어난 일만** 넘긴다. 분류(`자금부족`·
`상한제한`·`미주문`)는 **호출부가 명시**한다 — 사유 문자열을 뒤져서 분류하지
않는다. 증권사 거부 메시지에 "상한가" 같은 말이 섞여 들어오면 자금·상한과
무관한 건이 상한제한으로 둔갑하기 때문이다.

| 결과 | 뜻 | 미진입 확정? |
| --- | --- | --- |
| (빈 값) | 아직 아무 실행 결과도 받지 못함 | 아니오 |
| `미주문` | 게이트가 막아 주문 자체를 내지 않음 | 예 |
| `자금부족` | 가용현금 부족 | 예 |
| `상한제한` | 일일 상한 0 · 상관군/전체 유닛 캡 · 남은 자리 소진 | 예 |
| `상태미확인` | 전송 중 오류 등 — 주문이 나갔는지 모름 | **아니오** |
| `주문접수` | 접수는 확인, 체결은 별개 | **아니오** |
| `체결확인` | 체결 수량 확인 — 추적 종료 | 아니오 |

약한 결과가 강한 결과를 덮지 못한다(`_OUTCOME_RANK`). 특히 `주문접수`·
`상태미확인`이 나중의 `미주문`으로 덮여 화면에서 "미진입 확정"처럼 보이는
일이 없다.

`체결확인`은 **주문번호와 주문일이 그 추적 건의 것과 일치할 때만** 반영된다.
종목만 같은 옛 주문의 결과가 새 추적 건을 종료시키지 않는다.

### 종료

근거 있는 사건으로만 닫는다: **체결 확인**, 또는 **사용자 종료**.
**자동 만료 기간은 두지 않았다** — 적정 기간을 정할 근거가 아직 없어 임의의
숫자를 넣지 않았다. 미결정 사항이다.

### 야간 갱신 (`breakout_track_update.py`)

nightly-scan 워크플로에서 전종목 스캔 뒤 하루 한 번 돈다. `data/scan_latest.csv`의
**확정 종가만** 읽고 장중 현재가는 쓰지 않는다. 최초 값은 건드리지 않는다.

- `최초선대비ATR배수`의 분모는 **최초 ATR**이다 — 처음 신호와 같은 자로 재기
  위해서다. `돌파선까지ATR배수`의 분모는 **최신 ATR**이다("지금 기준으로 얼마나
  더 가야 새 돌파인가"). 분모가 다르므로 화면에서 둘을 함께 쓸 때 항상 밝힌다.
- `새돌파`는 **그날 종가 vs 당일을 제외한 20일 고가(`high20`)**로 본다.
  `high20_next`(당일 포함, 다음 거래일 기준선)와 비교하면 당일 고가가 자기
  자신을 못 넘어 새 돌파가 **항상 불가능**해진다.
- 같은 기준일로 이미 갱신된 건은 다시 쓰지 않는다(재실행 안전).
- 기준일이 미래거나, 같은 날 16시(`CLOSE_CONFIRMED_HOUR`) 전이거나, 최초 감지일
  이전이거나, 직전 기준일보다 과거면 갱신하지 않고 `갱신실패`로 남긴다 —
  직전 값은 지우지 않는다.
- 스캔 결과가 **통째로** 없으면 "야간 스캔 결과 없음 - 전체 갱신 보류"로 적는다.
  특정 종목만 스캔에 없는 경우(유동성 컷·거래정지·상장폐지)와 구분한다.

### 표시

- **웹앱** `돌파 후 미진입` 탭 — 목록(종목, 최초 감지일, 자동매수 결과, 최신
  가격·기준일, 최초선 대비 %·ATR, 새 돌파선까지 거리, 추적 상태)과 상세(최초
  기록, 현재 상태, 결과 사유, 사용자 메모). 읽기 전용이다. 조회가 실패하면
  "대상 없음"으로 보여주지 않고 실패했다고 적는다.
- **아침 리포트 메일** — 활성 건이 있을 때만 간결한 요약을 덧붙인다. 없으면
  섹션 자체를 생략한다.
- **카카오 알림은 추가하지 않았다.**

가격에는 항상 기준일을 함께 적는다(`2026-09-18 종가`). 장중 현재가와 혼동하면
안 된다.

## 5. 기존 매매 경로와의 격리

- 주문 경로(`kis_client`)에서 발생한 기록은 **메모리 큐에만 쌓이고**, 회차가
  끝난 뒤 `flush_events()`에서 한꺼번에 처리한다. 종목마다 Notion을 동기로
  부르면 그만큼 주문이 늦어지기 때문이다. 플러시는 목록 조회 1회 + 보유 조회
  1회 + 변경된 행당 쓰기 1회로 끝난다.
- `breakout_tracker`의 공개 함수는 **예외를 밖으로 내보내지 않는다.** 추적 DB가
  없거나 Notion이 실패해도 매매 판단과 주문 결과는 그대로다.
- `intraday_watch.run()`은 회차가 예외로 끝나도 큐를 비운다. dry-run에서는
  아무것도 기록하지 않는다.
- 주문 경로는 이 모듈을 **읽지 않는다** — 단방향으로 기록만 받는다. 과거
  돌파 신호로 주문이 생기는 경로는 없다.

## 6. 로컬에서 보기

```bash
# 계산만 하고 아무것도 쓰지 않는다
python breakout_track_update.py --dry-run

# 가상 데이터로 화면만 확인 (.env·운영 기록을 읽지 않는다)
streamlit run tools/breakout_preview.py
```

## 7. 아직 안 한 것

- 운영 Notion DB를 만들지 않았다. 시크릿도 넣지 않았다.
- 워크플로는 코드에 배선만 해 뒀고 수동 실행하지 않았다.
- SP삼화(000390)의 과거 돌파 이력은 **넣지 않았다.** 최초 감지 시각·그때의
  기준선·ATR을 남은 근거로 복원할 수 없어, 없는 값을 만들어 넣지 않는다.
  기능을 켠 뒤 다음 관측부터 쌓인다.
- 자동 만료 기간은 미결정이다.
