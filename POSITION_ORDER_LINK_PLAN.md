# 매도 주문–보유 포지션 연결 설계

작성: 2026-09-14. 상태: 설계 완료, 구현·Notion 스키마 변경·기존 데이터 이관·배포 미실행.

## 구현 선택

검토 대상은 PR #4의 `c8bb33a`와 별도 브랜치 `fix/auto-sell-recovery-review`의 `8aee408`이다. PR #4를 기반으로 필요한 보완을 구현한다. 별도 브랜치는 통째로 병합하지 않는다.

| 항목 | PR #4 | 별도 구현 | 선택 |
| --- | --- | --- | --- |
| 매도 기록 상태 | 접수는 주문중, 확인 후 성공 | 접수 성공과 별도 처리 결과로 복구 | PR #4 유지 |
| 포지션 구분 | 종목코드와 매수일 비교 | 종목코드와 매수일 이후 주문 조회 | 둘 다 관계 ID로 대체 필요 |
| 전날 미체결·부분체결 | 수동 확인 | 만료로 분류 후 다음 매도 판단 허용 | 자동 재주문 정책 도입하지 않음 |
| 청산 확인 | 전량 체결과 실제 보유수량 0 | 복구 경로에서 매도가능수량 검사, 직후 경로는 생략 가능 | 보유수량과 주문가능수량을 구분하는 PR #4 방식 유지 |
| 수량 유효성 | 일부 변환·비교에 의존 | 양의 정수/유한값 검사 함수 | 별도 구현의 검사 원칙을 독립 구현·테스트 |
| 이미 청산된 포지션 복구 | 현재 보유 목록에서 빠져 누락 가능 | 보유 목록 중심 처리 | 주문 목록 중심 복구로 개선 |

별도 구현의 만료 판정과 자동 재매도 전환은 선택하지 않는다. 주문번호 미확인, 조회 오류, 부분체결은 명확한 운영 결정 없이 자동 해소하지 않는다.

## 승인 요청 대상: Notion 속성 1개 추가

- 대상: `NOTION_ORDERS_DB_ID`가 가리키는 자동주문 기록 DB.
- 이름: `보유종목`.
- 유형: 단방향 relation.
- 연결 대상: `NOTION_DB_ID`가 가리키는 보유종목 점검표 DB.
- 관계는 각 매도 주문에 정확히 한 포지션만 저장한다. 서비스가 관계 개수를 제한하지 않아도 코드에서 1개를 검증한다.
- 보유종목 DB에 역방향 속성은 만들지 않는다. 기존 속성·레코드 값·주문 상태를 변경하지 않는다.
- 적용 직전 두 DB의 식별자와 현재 스키마를 읽어 검증한다. 같은 이름의 속성이 있으면 유형·대상이 일치할 때 재사용하고, 불일치하면 변경하지 않고 보고한다.
- 작성할 변경 요청은 해당 relation 속성 추가만 포함한다. 기존 스키마 전체를 덮어쓰지 않는다.

## 코드 계약

1. `run_auto_sell`이 보유 행의 page ID를 `place_market_sell_order`에 필수로 전달한다. `create_order_record`의 매도 사전 기록에 relation을 포함한다. relation이 없거나 유효하지 않거나 기록이 실패하면 주문을 전송하지 않는다. 매수 쪽 기존 호출 계약은 유지한다.
2. 미확인 매도 조회 결과에 `holding_page_id`, 주문일, 주문번호, 계좌, 종목, 수량, 상태를 반환한다. relation 누락과 복수 연결은 각각 별도 오류 상태로 취급한다.
3. 관계가 있는 주문은 page ID로만 포지션에 연결한다. 종목코드·계좌·운용 구분도 검증하고 불일치하면 자동 기록/주문을 하지 않는다. 날짜는 KIS 조회 범위에만 사용하며 포지션 식별에 사용하지 않는다.
4. 주문 복구를 현재 보유 목록 순회보다 먼저 수행한다. 현재 보유가 없어도 미확인 주문의 연결 page를 직접 조회하므로 이미 청산된 행의 마지막 기록 실패를 복구할 수 있다.
5. 같은 날 포지션 A 청산 후 포지션 B 재진입 시, A 주문은 B의 매도·추가매수 차단에 쓰지 않는다. 다만 A의 실제 체결이 아직 불명확하면 해당 종목 신규 진입은 보류한다. 관계만 있다는 이유로 증권사 잔여 주문 위험을 무시하지 않는다.

## 복구 순서와 재진입 처리

- 열린 포지션: 해당 주문의 유효한 요청 수량·체결 수량·체결가와 증권사 전체 잔고를 확인한다. 확인 완료 후 일지 기록 → 보유 청산 → 주문 성공 순서로 반영한다.
- 일지 존재 + 보유 청산 실패: 일지의 연결 포지션/주문 증거를 검증하고 재생성 없이 보유 청산만 재시도한다.
- 일지 존재 + 이미 청산 + 주문 상태 갱신 실패: 연결된 옛 포지션과 해당 일지의 주문번호·실제 체결 증거를 검증한 후 그 주문 상태만 마무리한다. 새로운 포지션의 잔고가 있다는 이유로 옛 주문을 다시 청산하려 하지 않는다. 일지 존재 여부 하나만으로 성공 처리하지 않는다.
- 일지 없음 + 이미 청산: 자동 기록 복구는 보류하고 확인 대상으로 보고한다. 종목 잔고 0만으로 과거 체결가나 수량을 추정하지 않는다.
- 여러 주문/부분체결: 주문별 체결과 포지션 전체 청산을 구분한다. 여러 체결을 합산한 일지 모델은 별도 작업이며 단일 주문으로 전체 포지션 일지를 위조하지 않는다.
- 삭제된 관계 대상·계좌/종목 불일치·잘못된 수량·조회 오류: 영향을 받는 항목을 격리하고 알린다. 독립적인 다른 포지션 복구는 계속한다.

## 기존 relation 없는 주문의 전환

관계가 없다고 안전한 주문으로 간주하거나 날짜만으로 자동 연결하지 않는다. 기존 주문을 자동 재분류·취소·성공 처리하지 않는다.

1. 승인 후 읽기 전용으로 relation 없는 미확인 매도 목록을 산출한다. 주문번호/일시/계좌/종목/수량과 보유·청산 행/일지 근거를 대조하여 후보 연결표를 만든다. 조회 결과에 비밀 값은 포함하지 않는다.
2. 연결을 확정할 근거가 충분한 주문도 실제 데이터 이관은 별도 승인 후 수행한다. 애매한 항목은 수동 확인 대상으로 남긴다.
3. relation 없는 미확인 주문은 해당 종목에 경고와 기존 보수적 차단을 유지한다. 새 포지션을 임의로 추정해 차단을 해제하지 않는다. 따라서 영향을 받는 운영 종목의 이관/확인이 끝나기 전에는 새 설계를 배포하지 않는다.
4. 과거 성공 주문은 이번 이관 대상에서 제외한다. 이미 잘못 청산 처리된 데이터를 소급 수정하지 않는다.

## 실행 순서

1. 본 설계 승인 후 로컬 구현과 네트워크 차단 테스트를 먼저 완료한다. 외부 주문/데이터를 건드리지 않는다.
2. 승인된 relation 속성 추가를 적용하고 읽기 전용으로 결과를 확인한다. 기존 데이터는 그대로 둔다.
3. 기존 미확인 주문 연결 후보표를 작성해 실제 이관 승인 여부를 받는다. 영향 종목이 없는 경우 그 근거를 기록한다.
4. Draft PR #4 코드·문서·테스트를 갱신하고 배포 준비 여부를 보고한다. main 병합·배포는 별도 승인 대상이다.
5. 승인된 병합 이후 자연 발생한 모의투자 실행으로 확인한다. 테스트 목적 주문·수동 워크플로 실행은 하지 않는다. 신규매수 상한 0 유지.

## 필수 테스트와 완료 기준

- 같은 날 A 청산 → B 재진입: A의 미완료 기록이 B의 정상 청산/추가매수를 막지 않음.
- 현재 보유 0건이어도 옛 연결 주문 상태 갱신 재시도 실행.
- 관계 없는 사전 기록, 관계 저장 실패, 복수 관계, 다른 종목·계좌 관계에서는 주문 전송 없음.
- 조회 실패·미체결·부분체결·잘못된 수량에서는 청산 확정 없음.
- 일지 생성/청산 갱신/주문 상태 갱신 각 실패 후 재실행에서 중복 일지·중복 주문 없음.
- relation 없는 기존 주문을 임의로 연결하거나 성공 처리하지 않음.
- 새 진입 차단과 포지션별 매도 차단이 서로 다른 목적에 맞게 동작함.
- 기존 신규매수 비용·가격 게이트와 상한 0 유지 테스트 통과.

## 되돌리기

추가한 속성은 비파괴적으로 남겨둔다. 데이터가 들어간 relation을 자동 삭제하지 않는다. 배포 후 단순히 구버전으로 되돌리면 종목/날짜 매칭 결함이 재발하므로, 오류 시 영향 기능의 운영 보류와 원인 확인을 먼저 진행한다. 운영 설정 변경 자체도 별도 승인 없이 수행하지 않는다.
