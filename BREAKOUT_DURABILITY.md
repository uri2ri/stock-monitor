# 추적 이벤트 영속 저장 및 복구

## 실제 저장 경로

매매 경로는 이벤트를 `data/breakout_outbox.json`에 직렬화하여 임시 파일 → fsync → 원자 교체로 기록한다. 최초 관측 시각(KST), 기준선·가격·ATR을 그대로 저장한다. 네트워크 요청·재시도는 하지 않으며 파일 잠금 경합은 기다리지 않고 명시적으로 실패한다. 로컬 경로는 `BREAKOUT_OUTBOX_PATH`로 테스트 격리가 가능하다. 운영 워크플로는 기본 경로만 사용한다.

Actions `auto-trade`의 실제 흐름:

1. checkout은 `github.ref_name`의 최신 커밋을 사용한다. 이전 회차가 push한 저널을 복원한다.
2. 매매/관측 이벤트를 로컬 저널에 기록한다. 매매 회차가 실패해도 후속 저장 단계는 `always()`로 실행한다.
3. 별도 prepare 단계가 읽기 조회와 메모리 계산만으로 처리 계획을 만든다. 초기값은 수정하지 않는다.
4. **Notion 쓰기 전에 저널을 commit/push**한다. push 실패 시 전달 단계는 실행하지 않는다.
5. deliver는 커밋된 파일과 실제 파일이 일치하는지 검사한다. 관측ID를 조회하고, 새 생성은 `creating` 표시를 파일 단독 commit/push한 뒤에만 POST한다. 표시 push가 실패하면 POST하지 않는다. 성공한 계획의 이벤트만 제거한다. 다른 계획의 실패는 유지한다.
6. 정리된 저널을 다시 commit/push한다. 실패하면 다음 실행은 마지막으로 성공한 원격 체크포인트(생성 직전 표시 포함)를 복원해 관측ID로 확인한다. 각 회차의 저널 artifact도 14일 보존한다(보조 수동 복구용, 자동 복원의 원본은 Git이다).

## 응답 유실과 중복 방지

별도 추적 DB의 `관측ID`(rich_text)는 최초 이벤트 UUID다. 생성 요청 전에 run ID/attempt가 든 계획을 Git에 보존한다. POST 응답이 유실됐거나 마지막 push가 실패해도 다음 회차는 같은 ID를 조회한다. ID와 최초 시각·종목이 일치하면 이미 저장된 것으로 확인하며 중복 생성하지 않는다.

**이전 회차 생성 여부가 불명확하고 조회에도 없으면 자동으로 POST를 재전송하지 않는다.** 조회 0건은 이전 생성 실패의 증거가 아니기 때문이다. 이 항목은 `ambiguous`로 남겨 수동 확인해야 한다. 요청 직전 중단돼 실제 POST를 한 번도 보내지 못했어도 같은 보수적 정책을 적용한다. Notion의 원자적 유일키 부재 때문에 가용성과 중복 없는 재전송을 동시에 보장한다고 주장하지 않는다.

동일 ticker의 미완료 계획이 있으면 새 이벤트를 먼저 반영하지 않는다. 기존 순서와 최초 관측값을 보존한다. 서로 다른 계획은 독립적으로 성공/실패를 기록한다. 수정 요청은 특정 관측 page ID에 묶고 늦은 재처리로 종료된 추적 건을 다시 열지 않는다.

새 계획은 `queued`로 저장되며 아직 POST하지 않았으므로 owner가 바뀌어도 처리할 수 있다.
`creating`/`ambiguous`는 생성이 불명확한 상태로 조회만 한다. 과거 형식의 `prepared`는
다른 owner가 자동 승계하지 않는다. 상태를 수동으로 `queued`로 바꿔 재전송하면 안 된다.
prepare/deliver 커서는 저널에 저장되며 회차마다 다음 위치부터 순환한다. 같은 종목의
후속 항목은 앞선 미완료 항목을 추월하지 않는다. 검사 건수 제한은 그대로다.

## 보존 한계 / 운영 전 조건

- 디스크 오류·잠금 경합·저널 10,000건 한도에서는 `이벤트 유실 가능`을 오류 로그에 남긴다. 매매는 중단하지 않는다.
- 로컬 기록 이후 **첫 Git push/artifact 업로드 전에 러너가 강제 종료되면 미보존 이벤트가 유실될 수 있다.** 파일만 기록했다고 실행 간 무손실을 보장하지 않는다.
- 체크포인트 push가 실패하면 다음 실행 전에 해당 artifact/실패 로그를 확인하고 복구해야 한다. artifact 보존 기간 이후까지 복구를 보장하지 않는다.
- 기존 `auto-trade` concurrency 그룹의 단일 writer를 전제로 한다. 다른 호스트에서 임의로 여러 worker를 실행하면 Notion 유일성을 보장할 수 없다.
- 연결할 추적 건이 없는 결과 이벤트나 보유 관측 때문에 새 추적을 만들 수 없는 이벤트도 성공으로 간주해 버리지 않는다. 저널에 보류하며 최초 관측/보유 근거를 수동 대조해야 한다.
- prepare는 회차당 최대 100개 이벤트, deliver는 최대 20개 계획과 60초 시작 예산으로 제한한다. 진행 중 HTTP 요청 시간은 이 예산을 넘길 수 있고 단계 자체는 워크플로 3분 제한을 둔다. 무제한 재시도 없음.
- 현재 운영 추적 DB/추가 속성/시크릿을 만들거나 변경하지 않았다. 별도 승인된 설정·배포 전까지 운영 미반영이다.
- 저널은 관측/주문 관련 데이터를 포함하므로 저장소 접근 권한을 검토해야 한다. 토큰·API키·실계좌번호는 저장하지 않는다.
